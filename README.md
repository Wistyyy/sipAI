Small Cli ai tool i made for learning purposes :)


### data class:
I create a blueprint (the bag) outside the function, just as a concept or idea, without filling it with actual values yet. The function itself calculates its own data, and at the end, it packs that data into a new bag based on this blueprint and returns it. This turns the pure concept into real, tangible data that I can use outside the function.

